<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PiFormance.Controls">

	<Style TargetType="local:ValueBar">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="local:ValueBar">
					<Viewbox Stretch="Uniform">
						<Border Background="{TemplateBinding Background}"
						        BorderBrush="{TemplateBinding BorderBrush}"
						        BorderThickness="{TemplateBinding BorderThickness}"
						        Height="30" Width="150">
							<StackPanel Orientation="Horizontal">
								<Rectangle Width="2" 
								           VerticalAlignment="Stretch"
								           Fill="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=SeparatorColor, Mode=OneWay}">
								</Rectangle>
								<Rectangle Width="3" 
								           VerticalAlignment="Stretch"
								           Fill="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Background, Mode=OneWay}">
								</Rectangle>

								<Rectangle VerticalAlignment="Center" 
								           HorizontalAlignment="Stretch"
								           Height="12"
								           Width="145"
								           Fill="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=BarColor, Mode=OneWay}">

									<!--<Rectangle.RenderTransform>
										<ScaleTransform>
											<ScaleTransform.ScaleX>
												<MultiBinding Converter="{StaticResource ValueToDoubleConverter}">
													<Binding Path="Value" RelativeSource="{RelativeSource TemplatedParent}"/>
													<Binding Path="Minimum" RelativeSource="{RelativeSource TemplatedParent}"/>
													<Binding Path="Maximum" RelativeSource="{RelativeSource TemplatedParent}"/>
												</MultiBinding>
											</ScaleTransform.ScaleX>
										</ScaleTransform>
									</Rectangle.RenderTransform>-->

								</Rectangle>
							</StackPanel>
						</Border>
					</Viewbox>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
		<Setter Property="Background" Value="#FF333333"/>
	</Style>

	<!--<Style TargetType="local:ValueBar" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ValueBar">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
</ResourceDictionary>
 
