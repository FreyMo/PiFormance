<UserControl
    x:Class="PiFormance.Client.Views.Gpu.GpuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PiFormance.Client.Views.Gpu"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition Height="Auto"/>
			<RowDefinition />
		</Grid.RowDefinitions>

		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		
		<local:GpuClocksControl Grid.ColumnSpan="2"/>

	    <local:GpuControl Grid.Row="1"
	                      Grid.Column="0" />

	    <local:VramControl Grid.Row="1"
		                   Grid.Column="1" />

	    <local:GpuLoadsControl Grid.Row="2"
	                           Grid.Column="0"
	                           Grid.ColumnSpan="2"/>

    </Grid>
</UserControl>
